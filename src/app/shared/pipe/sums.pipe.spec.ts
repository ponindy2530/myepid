/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { SumsPipe } from './sums.pipe';

describe('SumsPipe', () => {
  it('create an instance', () => {
    let pipe = new SumsPipe();
    expect(pipe).toBeTruthy();
  });
});
